# jotang-recruit-2026-git

**这是为焦糖工作室招新题而创建的 Git 学习仓库**

完成了git的下载和git bash命令行界面使用

命令行代码：
- `mkdir` 创建文件夹
- `cd` 定向文件夹
- `git init` 初始化
- `git remote add origin` 关联
- `git push` 上传
- `git status` 查看当前结果
- `git config --global http.proxy` 为git转网络代理连梯子

md里面怎么改字体（）

github仓库创建

改成有加粗和换行的git bash命令
